#ifndef _TREE_H
# define _TREE_H

#include <ncurses.h>
#include <time.h>
#include <stdlib.h>

// Local headers
#include "colors.h"
#include "posit.h"

void christTree(void);
int find_sol(int line, int *stat);

#endif

